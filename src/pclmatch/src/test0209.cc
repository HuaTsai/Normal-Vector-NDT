#include <bits/stdc++.h>

// #include <Eigen/Dense>
// #include <libconfig.h++>

#include "common/common.h"

using namespace std;

int main(int argc, char **argv) {
  // libconfig::Config cfg;
  // string gtbag_path;
  // cfg.readFile(APATH(20210209/params.cfg));
  // int iters = cfg.lookup("icp_params.max_iters");
  // double corrs = cfg.lookup("icp_params.max_corr_dists");
  // bool reci = cfg.lookup("icp_params.set_reci_corr");
  // double releps = cfg.lookup("icp_params.l2_rel_eps");
  // double tfeps = cfg.lookup("icp_params.tf_eps");

  // odometer
  // vector<vector<int>> raws = {iters, corrs, recis};
  // printf("%ld, %ld\n", raws.at(0).size(), raws.at(1).size());
  // vector<vector<int>::iterator> its;
  // for (auto &raw : raws) {
  //   its.push_back(raw.begin());
  // }
  // int k = its.size();
  // while (its.at(0) != raws.at(0).end()) {
  //   printf("%d, %d, %d\n", *its.at(0), *its.at(1), *its.at(2));
  //   ++its.at(k - 1);
  //   for (int i = k - 1; (i > 0) && (its.at(i) == raws.at(i).end()); --i) {
  //     its.at(i) = raws.at(i).begin();
  //     ++its.at(i - 1);
  //   }
  // }
}
